program main
    implicit none
    !double precision norm
    double complex im
    im = 3.0d0
    write(*, *) im
    !im = 2.0d0*im
    !norm = real(im)**2 + aimag(im)**2
    !write(*, *) norm
end program main